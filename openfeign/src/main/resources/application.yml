spring:
  application:
    name: openfeign
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:8848

server:
  port: 9191

# feign使用okhttp
feign:
  okhttp:
    enabled: true
